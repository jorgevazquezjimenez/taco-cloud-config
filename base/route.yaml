apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: "true"
  labels:
    app: taco-cloud
  name: taco-cloud
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: sanacloud-taco-cloud-poc
    weight: 100
  wildcardPolicy: None
